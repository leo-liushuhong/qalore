# 文本适配器

从需求文档、口述或任意文本输入中建立可测试的系统行为理解。

---

## 信息范围层级

用户提供的信息源（PRD/需求文档/口述）是本次任务的主输入，始终首先完整读取，不受本层级约束。

每次任务在以下范围层级内读取 story 文件。层级越高，范围越广，token 消耗越大。默认从层级 1 开始，每层严格包含上一层的全部范围。

**层级定义：**

```
层级 1 — 目标模块 + 1-hop 上下游 + 待确认项（安全基线，默认起始）
  目标模块：
    · BL 文件（完整）
    · BL changelog（近 3 条条目）
    · index.json 中 business_related 和 depends_on 条目
  1-hop 上下游（business_related + depends_on 中列出的每个模块）：
    · BL 文件的功能点节（从 `## 功能点：` 到下一个同级 `## ` 标题，
      不含 `## 代码` 开头和 `## 待确认` 开头的节）
    · 仅读断言行（`[assert-` 开头），不读代码独立断言节、代码来源表
    · 待确认项节（`## 待确认项` 下的 `[pending]` 行）
    · 若文件无 `## 功能点：` 节 → 该模块本次不提供 1-hop 信息，标注后跳过
  business_related 和 depends_on 均为空 → 层级 1 降为仅目标模块 + 待确认项，
  在探索报告中标注「1-hop 信息不可用，跨模块波及需依赖人工确认」
  格式规范：story-formats.md / assertions.md / changelog.md（按需加载）

层级 2 — + 测试用例
  在层级 1 基础上增加：目标模块及 1-hop 上下游模块的 TC 文件

层级 3 — + 代码
  在层级 2 基础上增加：目标模块及 1-hop 上下游模块的 CL 文件、code_paths 源代码

层级 4 — + 跨项目
  在层级 3 基础上增加：其他项目的 story 文件
```

**默认值与上限：**

| 信息源模式 | 起始层级 | 上限 |
|----------|--------|------|
| text-only | 层级 1 | 层级 2 |
| text+code | 层级 1 | 层级 3 |
| 用户指定 | 按指定 | 层级 4 |

**读取顺序：** 先目标模块 BL → 完成 7 问 → 再读 1-hop 上下游 → 执行波及与覆盖分析 → 发现缺口 → 升级层级。禁止在目标模块 7 问完成前读取任何其他模块的文件。

**层级升级**（满足任一条件，声明后升级）：

| 条件 | 说明 |
|------|------|
| 波及与覆盖分析发现缺口 | 答案在当前层级不可得 |
| 用户指出遗漏 | 缺失内容在更高层级 |

声明格式：`「当前层级 {N}，需读取 {内容} 以确认 {原因}。升级到层级 {N+1}？」`。用户同意 → 升级后仅在新内容上重跑波及与覆盖分析，追加或修正产出，不重跑 7 问。用户拒绝或已达上限 → 列为待确认项。

**探索报告**（仅层级升级时输出，不增加每次任务基础 token）：`「层级 {N}→{N+1}，补充读取 {模块列表}，新增 {n} 条断言 / {m} 条待确认」`

---

## 核心思考哲学：7问追问

> 7问追问是文本信息源的认知哲学，与 qa-understand SKILL.md 的三驱动元框架共同构成完整的认知体系：
> 三驱动决定「为什么读、何时停、如何识别边界」；7问追问决定「如何从需求描述中提取可测试行为」。

文本的语义单元是**功能点段落**（描述一个完整业务场景的最小文字单元）。对于多章节文档，导航优先从功能目标表或功能概览部分开始，这是信息密度最高、最能快速建立模块地图的位置。跨引用（「同当前规则」「见 X 功能」）按需追踪，最多一层。阅读策略见 qa-understand SKILL.md「渐进式阅读策略」章节。

对每个功能点段落逐一追问以下 7 个问题。目标不是复述文档写了什么，而是推导出什么可以被验证。

| 追问 | 目的 |
|------|------|
| **正常结果是什么？** 用户或系统能观察到什么可测量的变化？ | 功能类断言 |
| **它有哪些状态？** 每个状态下用户能感知到什么？ | 状态类断言 |
| **谁可以触发它？** 前提条件是什么？不满足时会怎样？ | 权限/前提类断言 |
| **输入是什么？** 格式/范围/长度/必填规则？违反时有什么反馈？ | 输入约束类断言 |
| **每个动作之后系统给什么反馈？** | 交互反馈类断言 |
| **它改变了什么？** 影响哪些其他模块或数据？ | 上下游联动类断言（跨模块时使用 `[模块::功能点]` 标签）|
| **什么情况下它会失败？** 失败后系统如何响应？ | 异常类断言 |

**追问原则：**
- 7问对每个需求对象全部执行，输入有描述 → 记录为断言；输入无描述 → 写入待确认项，不猜测
- story 里有 `[pending]` 待确认项时，在理解过程中自然解答，有答案则直接产出对应断言
- 一个追问可产生多条断言

**注意：** 第 6 问「它改变了什么」关注的是功能点对外产生的主动影响（谁依赖了它的输出）。波及分析关注的是反向——被删除或修改的功能点被谁引用和依赖——这在本表之外独立处理，见「波及与覆盖分析」章节。

---

## 波及与覆盖分析（产出前最后一步）

全部功能点完成 7 问追问后，对本次产出的整体做一次横向扫描。两条追问各问一次，覆盖整个模块，不逐功能点重复。

| 追问 | 目的 | 产出 |
|------|------|------|
| **波及** — 本次新增/修改/删除的需求对象，有哪些已有功能点引用或依赖了它？ | 识别需要同步更新的已有断言（3.2）或应删除的断言（3.3） | 波及清单：{断言ID} → {变更类型} |
| **覆盖** — 本次新增的行为，在系统的所有入口/渠道/用户角色中是否都需要生效？是否存在差异？ | 识别被遗漏的入口或角色 | 覆盖清单：{入口/角色} → {行为是否一致} → 不一致则补充断言，一致则标注 |

波及在目标模块内和 1-hop 上下游中寻找（如需求删除了某个角色 → 扫描 BL 文件中引用该角色的所有断言 → 波及）；覆盖在所有已知入口/角色中逐一核对（需求确认了哪些入口之后 → 每个入口逐个检查行为是否一致 → 有差异则逐入口补断言）。

两方向均离不开当前信息范围时 → 按「信息范围层级」章节的升级规则处理，不猜测，不跳过。

---

## 断言 ID 分配

规则遵循 story-formats.md「断言 ID 规范」（权威来源）：
- 从 index.json 的 `assert_seq` 获取当前最大序号，从 +1 开始递增
- **分配完成后立即更新 index.json 的 `assert_seq`**（将值设为本次分配的最大序号）。此更新不等【待沉淀】统一写入——立即执行，确保后续适配器（代码适配器）或 context 压缩恢复时读到正确的续接起点
- 更新方式：Read index.json → 修改 `modules.{模块名}.assert_seq` → Write 回文件（仅改此字段，不触及其他字段）
- 多源场景下【待沉淀】由 synthesis 统一输出，本节不单独输出

---

## 版本信号提取

阅读文档时，顺带识别版本信息（供 qa-functional-test 的 changelog 使用）：
- 优先取文档头部（标题行或元数据行）的版本号
- 头部无版本信号时，取文档中出现的数值最新版本号
- 无法确定时，在待沉淀的 index.json 声明中不填 `prd_version`，由 qa-functional-test 向用户确认

识别到的版本号写入交接块的 `信息源` 行（如 `文本（PRD v2.3）`），并在 index.json 声明中填入 `prd_version`。

---

## 写入目标格式

遵循 `{practices_path}/tech-stacks/functional/story-formats.md`「业务逻辑文件」格式规范。按需加载遵循 handbook.md「context 标记规范」章节。

---

## 产出

**单源（仅文本适配器运行）：** 输出【待沉淀】声明，格式遵循 `{practices_path}/common/handbook.md`「story 写入协议」：

```
【待沉淀】
| 文件 | 路径 | 操作 | 变更摘要 |
|------|------|------|---------|
| {模块名}-功能-业务逻辑.md | {story_path}/{项目}/{模块}/ | {新建/3.1新增/3.2更新/3.3删除} | {功能点变更摘要，如：新增「格式校验」(3条)，更新「图片上传」(1条)} |
| {模块名}-功能-业务逻辑.changelog.md | {story_path}/{项目}/{模块}/ | 只增不删 | {YYYY-MM-DD}（{来源描述}）{操作摘要} |
| index.json | {story_path}/{项目}/ | {新建条目/patch} | {变更字段摘要} |

新增/更新断言预览（供用户确认内容正确性）：
{逐行列出本次新增或更新的断言，格式与业务逻辑.md 相同，含 | upd:{版本/日期} 标记}
```

**写入规程：**
- 业务逻辑.md 按「断言变更规则」操作（见 handbook.md）：找到对应功能点章节原地覆盖，新功能点追加章节，删除的功能点整节移除
- 每条被新增或更新的断言行末附 `| upd:{版本/日期}`（有 PRD 版本用版本号，无则用日期）；功能点节头同步刷新 `> last-upd:{版本/日期}`
- **多源模式**（`【qa-understand-mode: multi】`）：在每条被新增或更新的断言行末 `| upd:{版本/日期}` 之后追加 `[来源:text]`，供 synthesis 区分来源
- **上下游断言跨模块时**：目标用 `[模块::功能点]` 标签标注；写入后**重建** index.json 的 `business_related`——扫描**步骤2合并后的完整内容**（即写入文件的最终版本）中所有 `[模块::功能点]` 标签，提取全量跨模块引用，完整替换 index.json 中的 `business_related` 字段（不追加，防止删除的引用残留形成幽灵数据）
- 内容文件写入后必须同步追加 changelog，格式见 `tech-stacks/functional/changelog.md`「断言 changelog 格式」章节
- **写入前计数**（组装待写入内容时同步完成，无需额外步骤）：待写入的 BL 文件内容在组装时自然可得断言总数（`[assert-` 行数）、功能点总数（`## 功能点：` 行数）、pending 数（`[pending]` 行数）——将这些值写入文件头部声明。禁止手算或预估
- **写入后验证**（每次 Write 后立即执行，仅一项）：`grep -c "^\[assert-" {文件路径}` vs 头部声明值 → 不一致则 Write 工具传输异常，立即重写

patch 操作说明：「现有 {m} 条」须来自实际 Read 文件后统计，不得从 context 记忆推断。

**多源（文本 + 代码适配器同时运行）：** 本节不输出独立的【待沉淀】。断言 ID 分配照常进行，执行结果（含断言列表和 pending 项）传入 synthesis.md，由其统一输出最终的【待沉淀】。

**index.json 声明字段：** 新模块首建时的完整字段 schema 见 `{practices_path}/schemas/story-index.schema.md`（权威来源）。已有模块更新时，仅声明变更字段：`prd_version`（若有版本信号）、`business_related`（有跨模块上下游断言时更新）、`status.pending_count`、`last_updated`（`assert_seq` 已在 ID 分配阶段实时更新，此处不重复声明）。首次为模块创建业务逻辑.md 时（无论模块是否已有其他 story 文件），额外声明：`status.business_logic: true`、`status.business_logic_changelog: true`。

> 模块 `status.tc_count = 0` 时，提醒用户：「{模块名} 尚无测试用例，如需生成执行『生成 {模块名} 用例』」
